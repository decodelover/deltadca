# Production API origin. Include /api because the frontend client targets that path.
VITE_API_BASE_URL=https://api.example.com/api

# Optional local-only proxy target for vite dev / preview.
VITE_PROXY_TARGET=http://localhost:5000
